package umc.spring.domain.enums;

public enum Gender {
// enum
// - 정해진 값들 중에 특정 값이 저장되는 상황에서 사용
// - 이런 값들은 타입을 String으로 하기 보다는 enum을 사용하는 것이 좋다
// - enum 역시도 클래스이기에, 생성자를 두는 등 여러 방식으로 사용이 가능하다

    MALE, FEMALE, NONE

}
